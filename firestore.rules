rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /books/{bookId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /users/{userId}/myList/{entryId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}

